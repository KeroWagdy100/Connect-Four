#include <iostream>
#include "Input.h"
#include "Game.h"
using namespace std;

int main() {
    // bool again{true};
    // do {
    //     playGame();
    //     cout << "-- Play Again? (Enter 0 or 1)\n>> ";
    //     Input::boolInput(again);
    // } while (again);
    Game g;
    return 0;
}
